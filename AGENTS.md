# AGENTS.md

## What this repo is

GitHub Pages **user site** (`jecisc.github.io`): everything at the root of `main` is published live at https://jecisc.github.io. Pushes to `main` go public immediately — never commit drafts, scratch files, or secrets.

Stated purpose (from README): personal page / blog.

## Current state

- Only tracked file is `README.md`. No build system, package manager, tests, linting, or CI exists yet — do not guess or run such commands.
- Single-branch workflow (`main`); no PR/release process in place.

## When tooling is added

Update this file with the real build/preview/test commands instead of assuming Jekyll defaults, even though GitHub Pages can serve Jekyll out of the box.
